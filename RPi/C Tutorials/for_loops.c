#include <stdio.h>

int main()
{
	/* Introducing the for loop */
	for(int i = 10 ; i > 0 ; i--)
	{
		printf("for loop count = %d\n", i);
	}
	printf("out of for loop now\n");
	
	/* A forever loop */
	//~ for(;;)
	//~ {
		//~ /* this loops forever */
	//~ }
	
	return 0;
}
